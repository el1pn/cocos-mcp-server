# Normalize line endings to LF in the repo and on checkout (consistent builds/diffs across OS).
# On Windows, if files still show as CRLF locally, run: git config core.autocrlf false
* text=auto eol=lf
